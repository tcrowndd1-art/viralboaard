---
date: 2026-05-09
type: plan
target: 자동 루프 + 홈/급상승 신선도 + 카드 UI + 탭 통합 설계
priority: high
status: open
triggered_by: David (5/9 오전 신선도 검수)
triggered_action: 0905-cycle1
version: v3 (Gemini 5/8 보고서 반영 — quota 폭발 회피)
affected_code:
  - "[[.github/workflows/daily-fetch.yml]]"
  - "[[backend/scripts/fetch_phase1.py]]"
  - "[[backend/scripts/run_niche.ps1]]"
  - "[[backend/scripts/run_cpm.ps1]]"
  - "[[src/pages/home/page.tsx]]"
  - "[[src/components/VideoCard]]"
  - "[[../../.gitignore]] (루트 Ai_Wiki)"
related_insights:
  - "[[2026-05-08-quota-diag]]"
  - "[[2026-05-08-issue-investigation]]"
---

# 0905-cycle1 — 자동 루프 + 신선도 + UI 통합 정비 (v3)

## v2 → v3 변경점 (5/8 Gemini 보고서 반영)

### 폐기 사유 — v2 G1.5 = 즉사
[[2026-05-08-quota-diag]]에서 **Windows Task Scheduler가 진짜 cron** (GitHub Actions 아님) 발견:
- ViralBoard_Fetcher: 30분 = 일 48회 × 2,809u = **134,832u/day**
- ViralBoard_Fresh: 1시간 = 일 24회 × 2,424u = **58,176u/day**
- 이론 일일 = **~201,524u** (7키 한도 70,000u의 **2.88배 초과**)
- 우연히 안 터지는 중: Niche/CPM DLL `-1073741510` 실패로 8.5K 절약 + 403 폭격으로 호출은 늘지만 저장 0
- v2 G1.5 (cron 8배) 실행 시 = **요청 1.6M unit/day = 즉시 사망**

### v3 핵심 변경
1. **G0 신설** (NEW, 최우선): quota 정밀 측정 + DLL 진단 + .gitignore 예외
2. **G1.5 → 0905-2차로 전면 보류** (cron 단축은 quota 정상화 후)
3. **G1 보완**: 메인 cron은 Windows TS — GitHub Actions 알림은 보조용. Windows TS 알림은 0905-2차
4. **G2 한정 적용**: 로컬 cron은 이미 KEY_1~7 사용 중 (`fetch_phase1.py:26-30` `range(1, 10)` 슬롯). 키 4~7 신규 등록 = **GitHub Actions 한정**
5. 보존: 보정 5개 + localStorage 캐싱 + 브랜치 + md: breakpoint = 그대로

### 실행 순서 v3
**G0 → G1 → G2 → G3 → G4 → G5** (G1.5는 0905-2차)

---

## 컨텍스트

5/9 오전 David 검수에서 발견한 5개 회귀 + 5/8 Gemini 보고서 2건 (quota 3배 초과, DLL 실패):

| 영역 | 문제 |
|---|---|
| 자동 루프 | Windows TS 30분/1시간 cron + 일 200K 설계 + 7키 한도 70K → 매일 오전 quota 소진 |
| 신선도 | shorts 218주 전, 누적조회수 라벨(1개월/3개월) ≠ SQL (필터 없음) |
| Rising 메트릭 | history 재진입으로 "며칠 평균"화 (issue-investigation 참조) |
| UI | 조회수/댓글/채널/구독자 노출 X, 썸네일 화질, 댓글 본문 X |
| 탭 중복 | 떡상탐지 ↔ 급상승 ↔ 인사이트 ↔ 채널랭킹 메트릭/테이블 분산 |

## 작업 범위 v3

| 순서 | ID | 그룹 | 우선순위 | 코드 변경 |
|---|---|---|---|---|
| 1 | **G0** | quota 정밀 측정 + DLL 진단 + .gitignore 예외 | 🔴 | 진단 위주 |
| 2 | G1 | GitHub Actions 가시성 (gh CLI + 텔레그램 알림) | 🔴 | yaml 1 step |
| 3 | G2 | YOUTUBE_API_KEY_4~7 GitHub secrets + workflow env | 🔴 | yaml 4줄 |
| ❌ | ~~G1.5~~ | ~~cron 단축~~ → **0905-2차 보류** | — | 0 |
| 4 | G3 | 홈 시간 필터 + 라벨 정합 + EmptyState | 🔴 | page.tsx 4 쿼리 |
| 5 | G4 | 카드 우측 메타 + 썸네일 실측 + 댓글 + localStorage | 🟡 | VideoCard + Modal |
| 6 | G5 | 탭 통합 설계 문서 (구현 X) | 🟢 | 0 |

## 진행 규칙

- 브랜치: `feature/0905-cycle1` (main 미푸시 1 commit 먼저 push 후 분기)
- 순차 (병렬 금지)
- ✅/❌ 보고만, raw 출력 ❌ (rejected-pattern #008)
- G별 commit 1회 이상, 막히면 즉시 정지 + David 호출

---

## G0 — quota 정밀 측정 + DLL 진단 + .gitignore (NEW)

### 목표
1. 5/8 보고서의 ~201K u/day 추정치를 5/9 실측으로 검증 (DLL 부활 후 폭발 막기 위한 근거)
2. Niche/CPM DLL `-1073741510` 원인 식별 (지금 고치면 안 됨, 원인만 파악)
3. 어제 작성한 5/8 분석 보고서 2건이 git 추적되도록 .gitignore 정리

### G0.1 quota 실측

```powershell
# 1. 오늘자 fetcher 로그 호출 횟수 정밀 측정
Select-String -Path "c:\Ai_Wiki\viralboard\backend\logs\fetcher_2026-05-09.log" -Pattern "^\[INFO\] Phase 1 시작" | Measure-Object
Select-String -Path "c:\Ai_Wiki\viralboard\backend\logs\fresh_2026-05-09.log" -Pattern "^\[INFO\]" -Encoding UTF8 | Measure-Object
Select-String -Path "c:\Ai_Wiki\viralboard\backend\logs\fetcher_2026-05-09.log" -Pattern "403" | Measure-Object

# 2. Windows Task Scheduler 5개 작업 실태
Get-ScheduledTask | Where-Object { $_.TaskName -like "ViralBoard_*" } | Select TaskName, State, @{N='LastRun';E={(Get-ScheduledTaskInfo $_).LastRunTime}}, @{N='LastResult';E={(Get-ScheduledTaskInfo $_).LastTaskResult}}
```

### G0.2 DLL `-1073741510` 진단 (수정 X, 원인만)

```powershell
# venv python.exe 절대경로 + DLL 의존성 확인
$venv = "c:\Ai_Wiki\viralboard\backend\.venv\Scripts\python.exe"
if (Test-Path $venv) {
    & $venv --version
    & $venv -c "import requests, supabase, dotenv; print('OK')"
}
```
- **금지**: venv 재생성, pip 설치, DLL 복사. 원인만 보고 후 정지.
- 부활 결정은 G0 후 별도 사이클 (quota 정상화 우선).

### G0.3 .gitignore 예외 추가

**확인 사항**:
- 루트 `c:/Ai_Wiki/.gitignore` → `viralboard/` 전체 제외 여부
- `viralboard/.gitignore` → `docs/analysis/` 추적 여부

**조치 (필요 시)**:
```gitignore
# c:/Ai_Wiki/.gitignore — viralboard 루트 제외 유지 + docs/analysis 예외
viralboard/
!viralboard/docs/
!viralboard/docs/analysis/
!viralboard/docs/analysis/**
```

→ 단, viralboard는 **별도 git repo** (.git 따로). 루트 Ai_Wiki repo에서 viralboard 추적 X가 의도적일 수 있음. **G0에서 두 .gitignore 모두 점검 후 사용자 컨펌 받고 수정**.

### 산출물
- `docs/analysis/2026-05-09-quota-actual-2026-05-09.md` (실측 결과 + DLL 진단)
- 필요 시 .gitignore diff 제안

### 일정
60분.

### 리스크
0 — 진단/관측만, 변경 X (.gitignore 제외, 그것도 컨펌 후).

---

## G1 — GitHub Actions 가시성

### 보완 (v3)
**메인 cron은 Windows TS** ([[2026-05-08-quota-diag#3]] 참조). GitHub Actions `daily-fetch.yml`은 KST 17:00 1회/일 보조. 이 G1 알림은 GitHub Actions 한정. **Windows TS 알림은 0905-2차** (별도 PowerShell + curl step 필요).

### 단계 + diff
v2와 동일. 요약:
1. `winget install --id GitHub.cli -e --accept-package-agreements --accept-source-agreements`
2. **정지 → David**: `gh auth login` 1회 직접
3. `gh run list --workflow daily-fetch.yml --limit 7 --json status,conclusion,startedAt,displayTitle`
4. workflow yaml 텔레그램 step 추가:
   ```yaml
         - name: Notify Telegram on failure
           if: failure()
           env:
             TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
             TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
           run: |
             curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
               -d "chat_id=${TELEGRAM_CHAT_ID}" \
               -d "text=❌ daily-fetch 실패 — github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
   ```
5. 검증: workflow_dispatch + 의도 실패 → 텔레그램 메시지 도착

### 전제 (점검 완료)
- ✅ `.env`에 TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID 라인 존재 (값 출력 X)
- secrets 등록은 G1 진입 시 `gh secret list`로 확인

### 일정 30분 / 리스크 낮음

---

## G2 — API KEY 4~7 GitHub secrets

### v3 한정 적용
**로컬 Windows TS는 이미 KEY_1~7 사용 중** (`fetch_phase1.py:26-30` `range(1, 10)` + `.env` KEY_1~7 라인 ✅ 점검 완료). G2는 **GitHub Actions 한정 보완** — `daily-fetch.yml` env 블록에만 KEY_4~7 4줄 추가.

### 단계 + diff
v2와 동일. 요약:
1. `gh secret set YOUTUBE_API_KEY_4` (값은 .env에서 추출, raw 출력 X)
2. KEY_5, 6, 7 동일 반복
3. `gh secret list | grep YOUTUBE` → 7개 라인 확인
4. workflow yaml env 4줄 추가:
   ```yaml
             YOUTUBE_API_KEY_4: ${{ secrets.YOUTUBE_API_KEY_4 }}
             YOUTUBE_API_KEY_5: ${{ secrets.YOUTUBE_API_KEY_5 }}
             YOUTUBE_API_KEY_6: ${{ secrets.YOUTUBE_API_KEY_6 }}
             YOUTUBE_API_KEY_7: ${{ secrets.YOUTUBE_API_KEY_7 }}
   ```
5. `gh workflow run daily-fetch.yml` → log artifact의 `[INFO] YouTube API 키 로드: 7개` 확인

### 일정 25분 / 리스크 낮음

---

## ~~G1.5 — cron 단축~~ → 0905-2차 보류

### 보류 사유
quota 3배 초과 상태. cron 단축 = 즉시 사망.

### 0905-2차 진입 조건
- G0 실측으로 일일 사용량 < 60K u/day 확인
- Windows TS Fetcher 30분 → 6시간, Fresh 1시간 → 3시간 (5/8 보고서 6장 권고)
- 그 후 cron 단축이 아닌 **현재 빈도 정상화** 우선

### 신선도 체감 회복 = G3로 충분
"218주 영상 노출", "1개월 라벨 ≠ SQL" 모두 시간 필터 부재 문제. 시간 필터 추가는 cron과 무관.

---

## G3 — 홈 시간 필터 + 라벨 정합 + EmptyState

### v3 보존 (v2와 동일)

**변경 파일**: `src/pages/home/page.tsx`

#### G3.1 Shorts (lines 643-686)
```tsx
.gte('published_at', new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString())
```
라벨: "Shorts 인기 (최근 30일)"

#### G3.2 누적조회수 (line 1015)
- shorts pool: published_at ≥ 30일전
- longform pool: published_at ≥ 90일전
- 정렬: views DESC

#### G3.3 실시간 트렌딩 (lines 688-751)
- `data.length < 4 fallback re-fetch` 제거 (line 705-713)
- 7일 필터 항상 유지

#### G3.4 떡상탐지 (Home Rising, lines 841-848)
- `published_at ≥ 30일전` → **`published_at ≥ 7일전`**

#### G3.5 EmptyState 컴포넌트 (Gemini 보정 #2)
```tsx
function EmptyState({ message }: { message: string }) {
  return (
    <div className="flex flex-col items-center justify-center py-12 text-zinc-500 dark:text-zinc-400">
      <p className="text-sm">{message}</p>
    </div>
  );
}
{trendingVideos.length === 0 && (
  <EmptyState message="데이터 부족 — 다른 카테고리를 시도해보세요" />
)}
```

### 검증
- localhost:3000 → 각 섹션 카드 published_at 시간 윈도우 안 벗어남 (DevTools)
- 카테고리 X 선택 시 EmptyState 노출
- 라벨과 데이터 정합

### 일정 75분 / 리스크 중간 (EmptyState로 UX 방어)

---

## G4 — 카드 UI + 썸네일 실측 + 댓글 + 캐싱

### v3 보존 (v2와 동일)

#### G4.1 카드 우측 세로 메타 (md+ only)
```tsx
<div className="flex flex-col md:flex-row">
  <Thumbnail />
  <div className="flex-1 flex flex-col">
    <Title />
    <div className="md:hidden">{/* 모바일 메타 (기존 유지) */}</div>
  </div>
  <aside className="hidden md:flex md:flex-col md:w-32 md:gap-2 md:pl-3 md:border-l md:border-zinc-200 dark:md:border-zinc-800">
    <MetaItem icon={Eye} label="조회수" value={formatViews(views)} />
    <MetaItem icon={MessageSquare} label="댓글" value={formatViews(comments)} />
    <MetaItem icon={User} label="채널" value={channel} truncate />
    <MetaItem icon={Users} label="구독자" value={formatViews(subscriberCount)} />
  </aside>
</div>
```

#### G4.2 썸네일 실측 (Gemini 보정 #3)
1. localhost:3000 → DevTools Network 탭 → `i.ytimg.com` 필터
2. 218주 전 (또는 published_at 5년 이상) 영상 카드 1개 picking — **G3 시간 필터 적용 후엔 사라지므로 G3 전 또는 다른 페이지에서 picking**
3. 첫 요청 `maxresdefault.jpg` 응답 코드 확인 (404 = 폴백 발동)
4. 두 번째 요청 `hqdefault.jpg` 200 확인
5. `docs/analysis/2026-05-09-thumbnail-fallback-test.md`에 기록 (영상 ID + 응답 코드)

#### G4.3 댓글 본문 + Quota 추정 + localStorage TTL 1h (Gemini 보정 #4 + Claude 강화 #6)

**Quota 추정**:
- `commentThreads.list` = 1 unit/req
- 영상당 1회 (모달 열 때)
- 회원 1,000명 × 모달 평균 10회 = 10,000 req/일 = 7키 quota의 14% 잠식
- → localStorage TTL 1h 캐싱 필수

```tsx
async function fetchYouTubeCommentsCached(videoId: string) {
  const key = `yt_comments:${videoId}`;
  const cached = localStorage.getItem(key);
  if (cached) {
    const { ts, data } = JSON.parse(cached);
    if (Date.now() - ts < 60 * 60 * 1000) return data;
  }
  const data = await fetchYouTubeComments(videoId);
  localStorage.setItem(key, JSON.stringify({ ts: Date.now(), data }));
  return data;
}
```

**0905-2차 보류**: Supabase `comments_cache` 테이블화.

### 검증

**Tier 2 검수** (Gemini 보정 #5 + viralboard CLAUDE.md):
- ✅ `max-w-7xl` 페이지 컨테이너
- ✅ `dark:bg-dark-base` 다크 모드
- ✅ Rising 페이지: `lg:ml-52` 사이드바 오프셋 (해당 시)
- ✅ `md:` breakpoint 분기로 모바일 회귀 0

**기능 검증**:
- localhost 데스크톱 → 카드 우측 세로 메타 4개
- DevTools 모바일 (375px) → 우측 hidden, 하단 메타 노출
- 썸네일 실측 결과 문서 첨부
- 영상 모달 → 댓글 5건 + localStorage `yt_comments:*` 키 저장
- 같은 영상 재오픈 → Network commentThreads.list 0회 (캐시 히트)

### 일정 100분 / 리스크 중간

---

## G5 — 탭 통합 설계 문서 (구현 X)

### v3 보존 (v2와 동일)
산출물: `docs/analysis/2026-05-09-design-tab-consolidation.md`

| 탭 | 데이터 | 메트릭 | 문제 |
|---|---|---|---|
| Rising | viralboard_rising | views_per_hour | 7일 + history 30분 fallback |
| 홈 떡상탐지 | viralboard_data | viral_ratio = views/subs | Rising과 다른 정의 |
| 채널 랭킹 | viralboard_data + history | subscribers DESC | 영상 X 채널 단위 |
| 크리에이터 인사이트 | viral_title_archive | viral_ratio ≥ 5 | archive 한정 |

**옵션 A** Single Source / **옵션 B** Layered / **옵션 C** 메트릭 칩
권고: **C → A 단계**. 0905-2차 = C 구현, 0905-3차 = A 도입.

### 일정 30분 / 리스크 0

---

## 일정 추정 v3

| 순서 | G | 시간 | 누적 |
|---|---|------|------|
| 1 | **G0** | 60분 (quota 실측 + DLL 진단 + .gitignore) | 1h |
| 2 | G1 | 30분 | 1h 30m |
| 3 | G2 | 25분 | 1h 55m |
| 4 | G3 | 75분 | 3h 10m |
| 5 | G4 | 100분 | 4h 50m |
| 6 | G5 | 30분 | 5h 20m |

총 **~5.5시간** (v2 4.5h → +1h G0).

## 0905-2차 보류 항목 (메모)

- Windows TS cron 빈도 정상화 (Fetcher 30분→6시간, Fresh 1시간→3시간) — 5/8 보고서 6장 권고
- Niche/CPM DLL `-1073741510` 부활 (venv 재생성)
- Windows TS 실패 알림 step (PowerShell + curl)
- 403 즉시 중단 분기 (`fetch_phase1.py:96-106`) — quota 보존
- Creator Insights "건강 5건" 문제 (KR 데이터 분포 + viral_ratio 임계값 카테고리별 조정)
- PlayBoard 식 풀 크롤 외부 조사 (Gemini CLI)
- 탭 통합 실제 구현 (G5 권고 옵션 C)
- 댓글 캐시 Supabase 테이블화

## 다음 단계

David 컨펌 → G0 진입 (PowerShell quota 측정 + DLL 진단부터, 코드/설정 변경 0).
